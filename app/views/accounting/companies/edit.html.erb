<% content_for :title, "Editar #{@company.name}" %>

<h1 class="text-2xl font-semibold tracking-tight text-ink">Editar empresa</h1>
<p class="mt-1 text-sm text-muted"><%= @company.name %></p>
<div class="mt-6 max-w-3xl">
  <%= render "form", company: @company %>
</div>
